{% extends 'base.html.twig' %}

{% block title %}Hello ProductsController!{% endblock %}

{% block body %}
    {% for product in products %}
        <li><a href="{{ path('product', {'id':product.id}) }}" >{{ product.name }}</a></li>
    {% endfor %}
{% endblock %}
