{% extends 'base.html.twig' %}

{% block title %}Hello ProductsController!{% endblock %}

{% block body %}
    {% for product in products %}
        <p><a href="{{ path('product', {'id':product.id}) }}" >{{ product.name }}</a><a href="{{ path('cart_add', {'id': product.id} ) }}">Ajouter au panier</a></p>
    {% endfor %}
{% endblock %}
