{% extends 'base.html.twig' %}

{% block title %}Mon Profil{% endblock %}
{% block stylesheets %}
{{ parent() }}
{% endblock %}

{% block body %}
    {% block nav %}
        {{ parent() }}
    {% endblock %}
    <h1>Choisir mes adresses de livraison et de facturation</h1>
        <div>
            <a href="{{ path('shipAdd_add') }}" class="btn btn-primary">Ajouter une adresse</a>
        </div>
        <div>
            {{ form(formSA) }}
        </div>
    {% block footer %}{{ parent() }}{% endblock %}
{% endblock %}